// // why data structure needed? 
// -> To arrange data in such a way that we can easily access data for CRUD

// // what is algorithm 
// -> steps to solve a specific problem

// // Bookshelf example for DSA

// // what does better mean? 
// --> Faster in execution 
// --> Less time intensive

// function sumUpTo(n) {
//     let total = 0;
//     for(let i = 0; i <= n; i++) {
//         total += i
//     }
//     return total;
// }

// console.log(sumUpTo(10))

// function sumUpTo(n) {
//     return n * (n + 1) /2
// }

// console.log(sumUpTo(10));

/////
// time is not constant, it varies according to ram, rom and even in same PC
// n number of operation depend on input, we can't count the operation

// How the operation grows based on input
// -> f(n) could be linear (f(n) = n)
// -> f(n) could be quadratic (f(n) = n^2)
// -> f(n) could be constant (f(n) = 1)
// -> f(n) could be logarithmic (f(n) = log n)
// -> f(n) could be loglinear (f(n) = n log (n))
// -> f(n) could be something entirely different!